# Changesets

This folder holds the changesets that drive releases. Run `pnpm changeset` and
describe what changed; the release workflow turns the accumulated changesets
into a version bump, a CHANGELOG entry and an npm publish.

See the [Changesets docs](https://github.com/changesets/changesets) for detail.
