---
"spoken-text": minor
---

Options written on a `<SpokenText>` inside a `<SpokenTextProvider>` are honoured
rather than ignored. The provider's own options are the defaults, so a
`debounceMs` can sit next to the text it is about.
